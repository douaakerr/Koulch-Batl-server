info:
  name: crete category
  type: http

http:
  method: POST
  url: http://localhost:3000/api/newCategory
  body:
    type: json
    data: |-
      {
        "name":"electronics"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​