info:
  name: get one
  type: http

http:
  method: GET
  url: http://localhost:3000/api/categories/:1
  params:
    - name: "1"
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
